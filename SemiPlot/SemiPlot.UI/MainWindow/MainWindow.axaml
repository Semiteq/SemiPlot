<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SemiPlot.UI.MainWindow"
        xmlns:chart="clr-namespace:SemiPlot.UI.Chart"
        xmlns:toolbar="clr-namespace:SemiPlot.UI.Toolbar"
        xmlns:legend="clr-namespace:SemiPlot.UI.Legend"
        xmlns:minimap="clr-namespace:SemiPlot.UI.Minimap"
        mc:Ignorable="d"
        d:DesignWidth="1280"
        d:DesignHeight="800"
        x:Class="SemiPlot.UI.MainWindow.MainWindow"
        x:DataType="local:MainWindowViewModel"
        Title="SemiPlot — Trend Viewer"
        Width="1280"
        Height="800"
        WindowStartupLocation="CenterScreen">

    <Grid RowDefinitions="Auto,*,Auto,Auto,Auto,Auto,Auto">

        <Border Grid.Row="0"
                Name="Toolbar"
                BorderThickness="0,0,0,1"
                BorderBrush="Gray"
                Padding="8,4">
            <toolbar:TrendToolbarView DataContext="{Binding ToolbarViewModel}" />
        </Border>

        <Grid Grid.Row="1"
              ColumnDefinitions="*,Auto">

            <Border Grid.Column="0"
                    Name="ChartContent"
                    Background="#1E1E1E">
                <chart:TrendChartView DataContext="{Binding ChartViewModel}" />
            </Border>

            <Border Grid.Column="1"
                    Name="LegendPanel"
                    Width="280"
                    BorderThickness="1,0,0,0"
                    BorderBrush="Gray"
                    Padding="4">
                <legend:TrendLegendView DataContext="{Binding LegendViewModel}" />
            </Border>

        </Grid>

        <minimap:MinimapView Grid.Row="2"
                             Name="Minimap"
                             Height="36"
                             DataContext="{Binding MinimapViewModel}" />

        <Border Grid.Row="3"
                Name="MessagePanel"
                BorderThickness="0,1,0,0"
                BorderBrush="Gray"
                Padding="8,4"
                IsVisible="{Binding IsCatalogueEmpty}">
            <TextBlock Name="EmptyCatalogueMessage"
                       Text="Pen catalogue is empty: the archive answered and semiplot_tags holds no pens. Ask whoever commissions this tool to fill it."
                       TextWrapping="Wrap"
                       VerticalAlignment="Center" />
        </Border>

        <Border Grid.Row="4"
                Name="ArchiveConnectionPanel"
                BorderThickness="0,1,0,0"
                BorderBrush="Gray"
                Padding="8,4"
                IsVisible="{Binding HasArchiveConnectionMessage}">
            <TextBlock Name="ArchiveConnectionMessage"
                       Text="{Binding ArchiveConnectionMessage}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center" />
        </Border>

        <Border Grid.Row="5"
                Name="ArchiveHealthPanel"
                BorderThickness="0,1,0,0"
                BorderBrush="Gray"
                Padding="8,4"
                IsVisible="{Binding HasArchiveHealthMessage}">
            <TextBlock Name="ArchiveHealthMessage"
                       Text="{Binding ArchiveHealthMessage}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center" />
        </Border>

        <Border Grid.Row="6"
                Name="StatusBar"
                BorderThickness="0,1,0,0"
                BorderBrush="Gray"
                Padding="8,4">
            <TextBlock Text="{Binding PenCount, StringFormat='Pens: {0}'}"
                       VerticalAlignment="Center" />
        </Border>

    </Grid>

</Window>
